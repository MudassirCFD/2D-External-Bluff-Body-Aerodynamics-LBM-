# Figures

This folder contains the selected final visual outputs from the LBM wake shape study.

The figures are post processed results only. Raw solver snapshot folders are not uploaded because they are large and can be regenerated by running the solver.
